package com.atc.domains.home;import com.atc.AbstractTestContext;import com.atc.common.model.home.LoginInfo;import com.atc.domains.home.entity.Task;import com.atc.service.home.impl.TaskServiceImpl;import com.atc.service.home.impl.UserServiceImpl;import org.junit.Assert;import org.junit.Test;import org.springframework.beans.factory.annotation.Autowired;import java.util.List;/** * Created by Vic.Feng on 04/12/2015. */public class TaskTest extends AbstractTestContext {    @Autowired    private TaskServiceImpl taskService;    @Test    public void testValidateUser() {        List<Task> tasks = taskService.getAllTasks();        Assert.assertNotNull(tasks);        Assert.assertTrue(tasks.size() > 0);    }}